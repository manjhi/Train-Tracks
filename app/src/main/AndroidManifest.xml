<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="manjinder.traintracks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TrainInfoByNumber"
            android:label="@string/Route"/>
        <activity android:name=".SeatAvailable"
            android:label="@string/Seat"/>
        <activity android:name=".LiveTrain"
            android:label="@string/Live"/>
        <activity android:name=".TrainBetweenStation"
            android:label="@string/Station"/>
        <activity android:name=".PNRStatus"
            android:label="@string/PNR">
        </activity>
        <activity android:name=".Station_to_Code"
            android:label="Station to Code"/>
    </application>

</manifest>